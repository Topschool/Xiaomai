<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.topschool.xm.dao.orderfood.OrderRecordDao">

    <!--auto generated Code-->
    <resultMap id="AllColumnMap" type="com.topschool.xm.model.OrderRecord">
        <result column="id" property="id"/>
        <result column="food_id" property="foodId"/>
        <result column="user_id" property="userId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="all_column">
        id,
        food_id,
        user_id,
        create_time
    </sql>

    <!--auto generated Code-->
    <insert id="insert">
        INSERT INTO order_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="pojo.id != null">id,</if>
            <if test="pojo.foodId != null">food_id,</if>
            <if test="pojo.userId != null">user_id,</if>
            <if test="pojo.createTime != null">create_time,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="pojo.id != null">#{pojo.id},</if>
            <if test="pojo.foodId != null">#{pojo.foodId},</if>
            <if test="pojo.userId != null">#{pojo.userId},</if>
            <if test="pojo.createTime != null">#{pojo.createTime},</if>
        </trim>
    </insert>

    <!--auto generated Code-->
    <insert id="insertList">
        INSERT INTO order_record(
        <include refid="all_column"/>
        )VALUES
        <foreach collection="pojos" item="pojo" index="index" separator=",">
            (
            #{pojo.id},
            #{pojo.foodId},
            #{pojo.userId},
            #{pojo.createTime}
            )
        </foreach>
    </insert>

    <!--auto generated Code-->
    <update id="update">
        UPDATE order_record
        <set>
            <if test="pojo.id != null">id = #{pojo.id},</if>
            <if test="pojo.foodId != null">food_id = #{pojo.foodId},</if>
            <if test="pojo.userId != null">user_id = #{pojo.userId},</if>
            <if test="pojo.createTime != null">create_time = #{pojo.createTime}</if>
        </set>
        WHERE id = #{pojo.id}
    </update>

    <!--auto generated Code-->
    <select id="select" resultMap="AllColumnMap">
        SELECT
        <include refid="all_column"/>
        FROM order_record
        <where>
            <if test="pojo.id != null">AND id = #{pojo.id}</if>
            <if test="pojo.foodId != null">AND food_id = #{pojo.foodId}</if>
            <if test="pojo.userId != null">AND user_id = #{pojo.userId}</if>
            <if test="pojo.createTime != null">AND create_time = #{pojo.createTime}</if>
        </where>
        LIMIT 1000
    </select>
    <select id="getTodayOrderByUserId" resultType="com.topschool.xm.model.OrderRecord">
        SELECT *
        FROM order_record
        WHERE user_id = #{uid} AND to_days(from_unixtime(create_time DIV 1000)) = to_days(#{currentTime});
    </select>

    <!--auto generated Code-->
    <delete id="delete">
        DELETE FROM order_record
        WHERE id = #{uid} AND food_id = #{foodId} AND
              to_days(from_unixtime(create_time DIV 1000)) = to_days(#{currentTime})
    </delete>

    <delete id="deleteTodayAll">
        DELETE FROM order_record
        WHERE id = #{uid} AND to_days(from_unixtime(create_time DIV 1000)) = to_days(#{currentTime})
    </delete>
</mapper>
